<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.ssd.a00818881.tinyarcade">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ViewGameplayActivity" />
        <activity android:name=".ViewContactActivity" />
        <activity android:name=".UpdateContactActivity" />
        <activity android:name=".GameplayHistoryActivity" />
        <activity android:name=".GameActivity" />
        <activity android:name=".AddContactActivity"></activity>
    </application>

</manifest>